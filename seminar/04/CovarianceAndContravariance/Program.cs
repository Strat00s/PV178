namespace CovarianceAndContravariance
{
    // Zadani:
    // Pro predpripravenou tridu Rental, predstavujici pujcovnu konkretniho typu vozidla,
    // vytvorte tridu implementujici rozhrani ICalculator. Metoda CalculateRentalPrice pak 
    // jednoduse zjisti hodnotu daneho vozidla, kterou pronasobi poctem dni na zapujceni a
    // pokud je vek ridice mimo rozmezi 24 az 80 let, nauctuje prirazku 20% k cene zapujceni.
    // Tridu Rental, ani metodu Main (krom instanciace kalkulatoru) nemuzete menit. Pred 
    // zacatkem implementace si nejdrive rozmyslete, zda pouzit kovarianci nebo kontravarianci.  
    class Program
    {
        static void Main(string[] args)
        {
            // var calculator = ...  TODO instantiate calculator

            // var carRental = new Rental<Car>(calculator);
            // carRental.RentVehicle(new Car(true), 1, 25);
            // carRental.RentVehicle(new Car(false), 2, 21);

            // var motorbikeRental = new Rental<Motorbike>(calculator);
            // motorbikeRental.RentVehicle(new Motorbike(), 3, 18);
            // Console.ReadKey();
        }
    }
}
